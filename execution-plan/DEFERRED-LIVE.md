# DEFERRED-LIVE — the single final round (build/deploy/live verification)

> Running ledger. Sessions append; nothing here blocks code. Executed as ONE round after all
> lakshmana code sessions complete (+ the vishwamitra sessions 00/07 the owner scoped separately).

| # | Item | Owner action / exact command | Added by |
| --- | --- | --- | --- |
| 1 | Terraform apply (lakshmana-infra) | `terraform init && terraform apply` after review; plan was deferred | setup |
| 2 | Model artifacts → GCS | rerun the LK-4 mirror script with the GCS destination (local `var/models/` cache is the interim source of truth) | setup |
| 3 | Neo4j `gatekeeper_ro` role + user + SM secret | cypher in lakshmana-infra README; store creds in Secret Manager | setup |
| 4 | Dispatcher + worker image push & deploy | build already verified locally; push to Artifact Registry, deploy Cloud Run service + job | setup |
| 5 | G4 live spot-check | small real flash-lite batch, spend counter vs billing (session06 DoD item, dry-run double used locally) | setup |
| 6 | Alert fault-injection | fire DLQ/job-failure/stuck-run/cap alerts once in the live project (session08/VA-104) | setup |
| 7 | vishwamitra sessions 00 + 07 | VA-77 dials · VA-106 integration (separate sessions, owner-scoped) | setup |
| 8 | SHADOW run + cutover | session08/VA-105 — needs 1–4 + 7 done | setup |
| 9 | `terraform init` + `validate` on the new TF root | `cd lakshmana-infra/terraform && cp backend.hcl.example backend.hcl && cp terraform.tfvars.example terraform.tfvars` (fill both) `&& terraform init -backend-config=backend.hcl && terraform validate`. **`fmt` is clean and the HCL parses; `validate` never ran** — `terraform init` is not in `.claude/settings.json`'s allow-list, so a headless session cannot download the provider. Recommend adding `Bash(terraform init:*)` (it is not infra-mutating; `apply`/`destroy`/`import` stay denied) so future sessions can self-verify. | session02 |
| 10 | Fill the tfvars the plan needs | `project_id`, `vishwamitra_state_bucket`, `dispatcher_image`, `worker_image`, `neo4j_uri` have no defaults and must be supplied. Confirm vishwamitra-infra's state really exports **`vpc_connector_id`** and **`notification_channel_ids`** — if the names differ, set the `vpc_connector_id` / `notification_channel_ids` variables instead of editing the root. | session02 |
| 11 | Images must exist before the first apply | Cloud Run will not create a service pointing at a missing image: push `gatekeeper-dispatcher` and `gatekeeper-worker` to Artifact Registry, *then* apply. Both build clean locally (verified session01 + session02). | session02 |
| 12 | Mirror the roster to `var/models/`, then pin the shas | `uv run --extra model-prep python scripts/prepare_models.py --roster all` (downloads from HF, exports ONNX INT8, runs the 50-pair sanity diff, writes `var/models/<name>/<version>/`). It prints one `manifest sha256` per artifact — put those in `gatekeeper.gates.g{1,2,3}.sha256`. Until then the loader logs "artifact is not pinned" and loads unverified. GCS upload is item 2 (add `--bucket`). | session02 |
| 13 | `hhem@v1` needs a hand-written export | It ships as a `trust_remote_code` architecture, so `optimum`'s sequence-classification path cannot export it; the prep script refuses it by design rather than quantizing the wrong graph. Not a blocker for VA-97 — `minicheck-deberta-l` and `factcg-deberta-l` cover the G2 slot. | session02 |
| 14 | Two alerts need log lines that do not exist yet | `gatekeeper/run_overdue` reads `jsonPayload.event="run_overdue"` (sweeper, VA-103) and `gatekeeper/cap_exceeded` reads `jsonPayload.errorCode="GK_E_CAP_EXCEEDED"` (G4, VA-102). Both metrics apply fine now and simply read zero; confirm they fire during the item-6 fault injection. | session02 |
| 15 | Artifacts are **fp32, not INT8** | All seven checkpoints failed the INT8 sanity diff and ship fp32 (LLD §8, O‑6). Both precisions are mirrored, so a future promotion needs no re-download. Live impact: item 2's upload moves ~5.6 GB rather than ~2.1 GB, and the worker job's 8 GB memory ceiling wants re-checking against the largest artifact (`minicheck-deberta-l` and `factcg-deberta-l`, 1.66 GB each) before the first real run. | session03 |
| 16 | Pin the artifact shas once the roster is chosen | Completes item 12's second half. The 7 manifest digests are in the session03 commit notes and in `var/models/<name>/v1/manifest.json`; set `gatekeeper.gates.g{1,2,3}.sha256` after the bake-off names a roster. Until then every load logs "artifact is not pinned". Note a demotion or a re-quantization **changes the digest** (the manifest covers `precision` and the sanity numbers), so pin after the roster is final, not before. | session03 |
| 18 | Upload ALL model artifacts to the bucket (owner directive) | extend item 2's mirror with `--roster all --bucket …` — v1 roster + every alternate, fp32 (+ INT8 when item 15 resolves); gates select by config, no code change | owner |
| 19 | Model pick + threshold calibration (the stopped bake-off) | resume `scripts/replay_bakeoff.py --all-rosters …` — per-artifact cache at `var/replay/scores/` already holds 4/7 (minicheck-L, nli-deberta-small, deberta-mnli-fever-anli, ettinx); remaining: modernbert, vitaminc, factcg; grid search afterwards is seconds. Closes O-1; O-2 additionally needs item 17's corpus | owner |
| 20 | **New IAM binding: the dispatcher SA needs `roles/monitoring.viewer`** | `/sweep`'s DLQ-depth check reads `pubsub.googleapis.com/subscription/num_undelivered_messages` through the Monitoring API, and `gatekeeper-dispatcher-sa` has no monitoring role in LLD §13's matrix. Add the binding in lakshmana-infra before the first live sweep. **Not urgent and not blocking:** the read is best-effort and a permission failure logs a warning and returns `dlqDepth: null` — the rescue passes, which are the part that matters, run regardless. The DLQ *alert* is a native GCP metric policy and is unaffected either way. | session04 |
| 21 | Configure the dispatcher's OIDC allow-list | `GATEKEEPER_DISPATCHER_ALLOWED_SERVICE_ACCOUNTS` must list `gatekeeper-push-sa@…` **and** the Cloud Scheduler SA, and `GATEKEEPER_DISPATCHER_OIDC_AUDIENCE` should be the deployed service URL. Both default to empty, which means "accept any Google identity" — the code logs a WARNING on every request in that state, deliberately. Verification is on by default (`GATEKEEPER_DISPATCHER_REQUIRE_OIDC=true`), so a deployment that sets neither still authenticates the caller, just does not authorize it. | session04 |
| 22 | Live spot-check the Run Jobs launcher | `CloudRunJobLauncher` calls `run_v2.JobsClient.run_job` with container overrides for `GATEKEEPER_RUN_REQUEST_ID` / `GATEKEEPER_GATE` / `GATEKEEPER_LEASE_OWNER`. Exercised locally only through `LoggingJobLauncher` and the inline test double — the real API call has never run. Turn it on with `GATEKEEPER_WORKER_EXECUTE_JOBS=true` (it is off by default so a local dispatcher cannot reach the Run Admin API by accident) and confirm one execution starts with the right three variables. **Terraform must not bake those three into the job's own env** — an override replaces the value, and a stale baked-in `GATEKEEPER_GATE` is what a hand-run execution would pick up. | session04 |
| 23 | `gatekeeper_pairs` is a new collection in the shared Firestore database | Lakshmana now owns a collection in vishwamitra's database (LLD §7.3, O‑8). **No composite index is required** — the gate loop's candidate query is equality-only and Firestore serves it by merging single-field indexes. Nothing to apply; this is a "be aware it exists" item, worth a line in vishwamitra's data-model notes. The `(stage3RunId, tier, gate, leaseExpiresAt)` composite the old §7.5 listed is **not** needed and should not be added. | session04 |
| 24 | Emulator-only proof for the queue's index-free query | The equality-only candidate query has been exercised against the Firestore **emulator**, which does not enforce index requirements the way production does. If production rejects it, the fix is one composite index on `gatekeeper_pairs (stage3RunId ASC, gate ASC)` — cheap, but confirm during the first real run rather than being surprised by it. | session04 |
| 17 | **The GO bar's contradiction criterion is unproven** — the replay corpus contains none | The exported corpus (12,208 pairs, the 209-claim reference subject) holds **zero golden pairs and zero CONTRADICTS verdicts**: that vishwamitra run judged every pair bare and the ensemble never returned a contradiction. So §15's "CONTRADICTS recall ≥ 95% of the ensemble's own" has an empty denominator and is reported **unmeasurable**, not passed — the bake-off scores it neither way (`goBar.unmeasurable` in the archived report). The escape hatch and G3 cross-check are exercised by unit tests, never by real contradicting claims. **Before cutover**, export a second corpus from an intake whose ensemble did return CONTRADICTS (and ideally `withContext=true` pairs, also absent here) and re-run `scripts/replay_bakeoff.py` against it; that run, not this one, closes LLD O‑2. | session03 |
| 25 | Confidence is uncalibrated — the mapping is the identity | LLD §8 says `confidence = calibrated(score)` for G2 and the §11.10 queue floors on G3's. No calibrator is fitted (item 19), so both ship as **raw scores, honestly labelled**: G2 stores the winning support probability, G3 stores `min(famA, famB)`. `tests/test_g2_gate.py` pins the row against whatever `decide_g2` produced, so fitting a curve later changes one function and the test still holds. The session05 DoD's "spot-check against the calibration table" is **unmeasurable until item 19 produces a table** — reported that way rather than vacuously passed, exactly as item 17 handles the GO bar's contradiction criterion. | session05 |
| 26 | Confirm `claims.sourceExcerpt` coverage on real data | G2's grounding mode is the one thing the cascade can do that the LLM judge could not, and it only fires for pairs whose claims carry a `sourceExcerpt` (LLD §8 G2, O‑9). Coverage on the real corpus is **unknown** — the excerpt is LLM-emitted at Stage 2 and null for audio-derived claims. Before cutover, count `claims` docs with a non-empty `sourceExcerpt` for a real subject: if coverage is low, grounding mode is decoration and G2's `supportMin` wants calibrating on the claim-vs-claim path alone. The `grounded` counter on every run doc reports this per-run once live. | session05 |
| 27 | Neo4j password is not in the repo's settings env | `tests/test_e2e_cascade.py::test_the_hydration_cypher_runs_against_a_real_neo4j` is the only test that proves the cypher parses against a real server, and it skips unless `GATEKEEPER_NEO4J_PASSWORD` is set. Session05 fixed the test itself (it passed `load_config({})`, an empty env map, so the variable could **never** arrive and it skipped unconditionally). Add `"env": {"GATEKEEPER_NEO4J_PASSWORD": "vishwamitra-dev"}` to `.claude/settings.local.json` to take the suite to 0 skipped locally. Verified passing against the running container when the variable is present. **Corrected in session06: `.claude/settings.local.json` is NOT gitignored** — it is a tracked file, so the fix as written would commit a container credential to the repo. Either `git rm --cached .claude/settings.local.json` and add it to `.gitignore` first (the usual convention for a `*.local.*` file, and what the item assumed), or export the variable from the shell instead. Session06 declined to write the credential for this reason and re-verified the path another way: a scratch smoke drove the real `worker.main()` against the live container read-only with the variable set, and G1's hydrate cypher executed cleanly. | session05, corrected session06 |
| 28 | **G4 has never called Vertex — the live door is unexercised** | `gatekeeper/clients/vertex.py`'s `VertexGeminiClient` builds ADC credentials and POSTs `generateContent` at `{region}-aiplatform.googleapis.com`, mirroring vishwamitra's `VertexGeminiTransport`. Locally it is *never* constructed: `gatekeeper.gates.g4.live-calls` defaults **off**, so `client_for` returns the dry-run double and the whole cascade runs without spending. Turn it on with `GATEKEEPER_G4_LIVE_CALLS=true` on a small real batch and confirm (a) the call succeeds against the pinned row, (b) `usageMetadata` carries `promptTokenCount`/`candidatesTokenCount`/`thoughtsTokenCount` as the parser expects, (c) the run doc's `llmSpendUsd` lands within 5% of billing (the session06 DoD item, unmeasurable locally). Supersedes/completes item 5. | session06 |
| 29 | **Confirm the flash-lite pin and its price rates** | `gatekeeper.gates.g4.llm-model` now defaults to `gemini-2.5-flash-lite` (it was the placeholder `pending-lite-pin`, which no live call could resolve). The LLD had already chosen flash-lite; session06 only made the row concrete. Confirm it against what vishwamitra's admin-managed `providers/stage3-judge` row is pinned to — **deliberately not read by lakshmana**, since a SHADOW comparison where one judge silently follows the other's pin measures nothing. Check `gatekeeper.gates.g4.usd-per-million-input` (0.10) and `-output` (0.40) against current published rates at the same time: they are config precisely because prices move, and `llmSpendUsd` is only as honest as those two numbers. | session06 |
| 30 | **`thinkingBudget` vs `thinkingLevel` is a pin-shaped trap** | `thinking_config()` ports vishwamitra's `GeminiThinking.config`: Gemini 3.x replaced the integer `thinkingBudget` with the enum `thinkingLevel` and **silently ignores the wrong knob**. So repinning item 29's model from a 2.5 row to a 3.x row without the port would quietly restore full thinking and multiply the tail's bill with no error anywhere. The port is unit-covered but has never been checked against a real 3.x response. Verify during item 28 if the pin is ever moved to a 3.x row. | session06 |
| 31 | **`extraction_prompts/STAGE3_JUDGE` is a new read dependency** | G4's prompt resolves the judge rubric from vishwamitra's own prompt registry (`ExtractionPromptRepository.COLLECTION`, doc `STAGE3_JUDGE`, field `instructions`), read-only, falling back to a vendored mirror of the YAML default when the row is absent or blank — the same resolution `ExtractionPromptService.resolveKey` performs. This is what keeps a SHADOW comparison honest: an admin rubric edit has to reach both judges or they are answering different questions. Worth a line in vishwamitra's data-model notes alongside O‑8/O‑9. Confirm the row exists in the live project before the SHADOW run; if it does not, both judges fall back to their own copies and those copies must not have drifted. | session06 |
| 32 | **`shadowDisagreed` is written as 0 and VA-105 owns the real number** | §7.1 lists it in `totals`; FINALIZE writes 0 in every mode. In GATEKEEPER that is simply correct. In SHADOW the honest number cannot be computed at finalize time: vishwamitra's ensemble runs concurrently and may not have judged a pair yet, so any count taken then races the ensemble with a systematic bias toward zero. The disagreement report is VA-105's offline pass over the durable rows (`shadow.verdict` on the `\|GK` row vs the ensemble's own row for the same pair, joined on `subjectId` — note the ensemble row carries **no** `stage3RunId`, so `subjectId` is the only join key). Do not read the finalize-time zero as evidence of agreement. | session06 |
